
HideObject
隐藏一个单一物件。隐藏物件，使其不可见、不可捕捉、不可选择。
语法
rhinoscriptsyntax.HideObject (object_id)
rhinoscript.object.HideObject (object_id)
参数




object_id
必须参数。字符串或 Guid。要隐藏物件的 ID 。


返回值




布尔值
True 或 False 表示计算完成或失败。


示例
import rhinoscriptsyntax as rs
id = rs.GetObject("Select object to hide")
if id: rs.HideObject(id)
同见
HideObjects
IsObjectHidden
ShowObject
ShowObjects
