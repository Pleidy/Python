
LockObjects
锁定一个或多个物件。锁定的物件可见，也可以被物件锁点捕捉。但是，不能被选择。
语法
rhinoscriptsyntax.LockObjects (object_ids)
rhinoscript.object.LockObjects (object_ids)
参数




object_ids
必须参数。列表。要锁定物件的 Guid 或字符串构成的列表。


返回值




数字
被锁定物件的数量。


示例
import rhinoscriptsyntax as rs
ids = rs.GetObjects("Select objects to lock")
if ids: rs.LockObjects(ids)
同见
IsObjectLocked
LockObject
UnlockObject
UnlockObjects
 
