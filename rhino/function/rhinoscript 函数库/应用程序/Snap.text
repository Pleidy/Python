
Snap
打开或关闭  Rhino 的锁定格点。
语法
rhinoscriptsyntax.Snap( enable=None )
rhinoscript.application.Snap( enable=None )
参数




enable
可选参数。布尔值。打开（True）或关闭（False）。


返回值




布尔值
如果指定了 enable 参数，返回当前的锁定格点设置状态。


布尔值
如果没有指定 enable 参数，返回先前的锁定格点设置状态。


示例
import rhinoscriptsyntax as rs
if not rs.Snap(): rs.Snap(True)
同见
Ortho
Osnap
Planar
 
