
ProjectOsnaps
打开或关闭物件锁点投影。
语法
rhinoscriptsyntax.ProjectOsnaps( enable=None )
rhinoscript.application.ProjectOsnaps( enable=None )
参数




enable
可选参数。布尔值。打开（True）或关闭（False）。


返回值




布尔值
如果没有指定 enable 参数，返回当前物件锁点投影的设置。


布尔值
如果指定了 enable 参数，返回先前的物件锁点投影设置。


示例
import rhinoscriptsyntax as rs
if not rs.ProjectOsnaps(): rs.ProjectOsnaps(True)
同见
Osnap
OsnapDialog
OsnapMode
 
